// Add functionality to the login button
document.getElementById('login-button').addEventListener('click', () => {
    // Redirect to Discord OAuth2 login (replace with actual URL)
    window.location.href = '/auth/discord';
});
